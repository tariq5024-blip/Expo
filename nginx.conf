server {
    listen 80;
    server_name _;

    root /var/www/expo/client/dist;
    index index.html;

    # Frontend SPA
    location / {
        try_files $uri /index.html;
    }

    # API reverse proxy (Web VM -> App VM)
    # Update upstream IP/port if your app server changes.
    location /api/ {
        proxy_pass http://10.96.133.197:5000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static uploads served from backend
    location /uploads/ {
        proxy_pass http://10.96.133.197:5000/uploads/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
